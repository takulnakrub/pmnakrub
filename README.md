# 🔥 ไม้ขีด - ระบบรายงานภัยสิ่งแวดล้อมอัจฉริยะ

## 🎯 ภาพรวม

ไม่ใช่แค่เว็บแจ้งเหตุธรรมดา! นี่คือ **ระบบอัจฉริยะ** ที่รวม 3 เทคโนโลยีหลักเข้าด้วยกัน:

### ✨ ฟีเจอร์หลักทั้ง 3 อย่าง

#### 1. 🤖 AI Image Screening (คัดกรองรูปภาพด้วย AI)
- ใช้ **Claude API** วิเคราะห์รูปภาพอัตโนมัติ
- ตรวจจับ: ควัน, ไฟ, เปลวไฟ, การเผา, มลพิษทางอากาศ
- แสดงผลลัพธ์:
  - ✅ **ตรวจพบภัย** (Confidence ≥ 70%) - อนุญาตให้ส่งรายงาน
  - ⚠️ **พบภัยไม่แน่ชัด** (Confidence 40-69%) - แนะนำตรวจสอบเพิ่ม
  - ❌ **ไม่พบภัย** (Confidence < 40%) - บล็อกไม่ให้ส่ง
- ลดรายงานปลอมได้อย่างมีประสิทธิภาพ

#### 2. ✅ Community Verification (ยืนยันจากชุมชน)
- **ให้ผู้ใช้คนอื่นช่วยยืนยัน** รายงานที่ผ่าน AI แล้ว
- รับ **5 โทเค็น** ทุกครั้งที่ยืนยัน
- ต้องมีคนยืนยัน **อย่างน้อย 2 คน** ถึงจะถือว่ารายงานนั้น "verified"
- กรองรายงาน:
  - ผ่าน AI แล้ว
  - ยังไม่ถึง 2 คนยืนยัน  
  - ไม่ใช่รายงานของตัวเอง
- แสดง Badge จำนวนรายงานที่รอการยืนยัน

#### 3. 🔥 Heatmap (แผนที่ความร้อน)
- ใช้ **Leaflet.heat** แสดงความหนาแน่นของจุดรายงาน
- สีเปลี่ยนตามความหนาแน่น:
  - 🔵 สีน้ำเงิน = น้อย
  - 🟡 สีเหลือง = ปานกลาง
  - 🔴 สีแดง = มาก
- มี Legend อธิบายสี
- สลับ เปิด/ปิด ได้

---

## 🚀 วิธีการทำงาน

### ขั้นตอนการแจ้งเหตุ

1. **เลือกประเภทเหตุการณ์**: เผาขยะ, ไฟป่า, ควันรถ, โรงงาน
2. **ถ่ายรูป/อัปโหลดรูป**: จำเป็นต้องมี!
3. **AI วิเคราะห์อัตโนมัติ**: 
   - แสดงผล loading animation
   - ส่งรูปไปที่ Claude API
   - รับผลการวิเคราะห์ภายใน 2-3 วินาที
   - แสดงผลลัพธ์ พร้อมความมั่นใจ
4. **ปักหมุดตำแหน่ง**: ใช้ GPS หรือลากหมุดปรับเอง
5. **ส่งรายงาน**: ถ้าผ่าน AI (confidence ≥ 40%)

### ขั้นตอนการยืนยันจากชุมชน

1. **กดปุ่ม "ยืนยันรายงาน"**
2. **เห็นรายการ** รายงานที่:
   - ผ่าน AI screening แล้ว
   - ยังไม่มีคนยืนยันครบ 2 คน
   - ไม่ใช่รายงานของตัวเอง
3. **ดูรูปภาพ** + ผลการวิเคราะห์ของ AI
4. **เลือก**:
   - ✅ **ยืนยัน** → ได้ 5 โทเค็น
   - ❌ **รายงานผิด** → ไม่ได้โทเค็น

### ขั้นตอนการดู Heatmap

1. **กดปุ่ม "Heatmap"** 
2. **ดูพื้นที่เสี่ยง** ที่มีรายงานหนาแน่น
3. **สังเกตสี**: น้ำเงิน → เหลือง → แดง
4. **กดอีกครั้ง** เพื่อปิด

---

## 📊 ระบบคะแนนและรางวัล

### การได้โทเค็น
- แจ้งเหตุที่ผ่าน AI: **15-25 โทเค็น** (สุ่ม)
- ยืนยันรายงานของคนอื่น: **5 โทเค็น**

### แลกของรางวัล
- ส่วนลดค่าไฟ 20 บาท: **50 โทเค็น**
- บัตรของขวัญ 50 บาท: **100 โทเค็น**
- ปลูกต้นไม้ 1 ต้น: **200 โทเค็น**

---

## 🎨 ไฟล์ในระบบ

```
📁 ระบบไม้ขีด
├── 📄 index.html              # หน้าเว็บหลัก (มี AI, Verification, Heatmap)
├── 📄 app.js                  # JavaScript (AI API, Heatmap, Verification)
├── 📄 style.css               # CSS หลัก
├── 📄 store_styles.css        # CSS ร้านค้า
├── 📄 extended_styles.css     # CSS เพิ่มเติม (AI, Verification, Heatmap)
└── 📄 README.md               # ไฟล์นี้
```

---

## 🔧 เทคโนโลยีที่ใช้

### Frontend
- **HTML5** + **CSS3** (Glassmorphism Design)
- **Vanilla JavaScript** (ไม่ใช้ framework)
- **Leaflet.js** - แผนที่
- **Leaflet.heat** - Heatmap

### AI & Backend
- **Claude API (Sonnet 4)** - วิเคราะห์รูปภาพ
- **Google Apps Script** - Backend (OTP, บันทึกข้อมูล)
- **Google Sheets** - Database

---

## ⚙️ การตั้งค่าก่อนใช้งาน

### 1. ตั้งค่า Anthropic API Key

เปิดไฟล์ `app.js` แล้วแก้ไขบรรทัดที่มี:

```javascript
"x-api-key": "ANTHROPIC_API_KEY_PLACEHOLDER"
```

เป็น:

```javascript
"x-api-key": "sk-ant-api03-xxx_YOUR_ACTUAL_API_KEY_HERE"
```

**วิธีได้ API Key:**
1. ไปที่ https://console.anthropic.com/
2. สมัครสมาชิก (ถ้ายังไม่มีบัญชี)
3. ไปที่ Settings → API Keys
4. สร้าง API Key ใหม่
5. คัดลอกมาใส่ในโค้ด

**⚠️ สำคัญ:** API Key นี้เป็นความลับ! อย่าแชร์หรือ commit ขึ้น Git

### 2. ตั้งค่า Google Apps Script (สำหรับ OTP & Database)

ไฟล์ที่ต้องแก้:
- `app.js` → แก้ `SHEET_URL` ให้ตรงกับ Google Apps Script URL ของคุณ

---

## 🎮 การใช้งาน (หลังตั้งค่าเรียบร้อย)

### Features
- 📱 Responsive Design
- 🎨 Modern Glassmorphism UI
- 🔐 OTP Login System
- 📍 GPS Integration
- 🗺️ Interactive Map
- 🔥 Real-time Heatmap
- 🤖 AI-Powered Screening
- ✅ Community Verification

### ขั้นตอน

1. เปิดเว็บ → Login ด้วย OTP
2. กด "แจ้งเหตุทันที"
3. อัปโหลดรูป → รอ AI วิเคราะห์ (2-3 วินาที)
4. ถ้าผ่าน → เลือกประเภท + ปักหมุด → ส่ง
5. ได้โทเค็น!
6. กด "ยืนยันรายงาน" → ช่วยตรวจสอบของคนอื่น → ได้โทเค็นเพิ่ม!
7. กด "Heatmap" → ดูพื้นที่เสี่ยง

---

## 💡 จุดเด่นของระบบ

### 1. ลดรายงานปลอม
- AI คัดกรองก่อนส่ง
- ชุมชนยืนยันอีกชั้น
- ระบบ 2 ชั้นทำให้ข้อมูลน่าเชื่อถือ

### 2. กระตุ้นการมีส่วนร่วม
- ได้โทเค็นจากการยืนยัน
- ทำให้คนอยากช่วยตรวจสอบ
- สร้างชุมชนที่ช่วยเหลือกัน

### 3. มองเห็นภาพรวม
- Heatmap แสดงพื้นที่เสี่ยงชัดเจน
- Stats แสดงผลแบบ real-time
- ช่วยในการวางแผนแก้ปัญหา

---

## 📈 สถิติที่แสดง

- **รายงานทั้งหมด**: จำนวนรายงานทั้งหมดในระบบ
- **ยืนยันแล้ว**: รายงานที่มีคนยืนยัน ≥ 2 คน
- **ความแม่นยำ AI**: % ของรายงานที่ยืนยันแล้ว / รายงานทั้งหมด

---

## 🎯 การใช้งาน

1. เปิดเว็บ → Login ด้วย OTP
2. กด "แจ้งเหตุทันที"
3. อัปโหลดรูป → รอ AI วิเคราะห์
4. ถ้าผ่าน → เลือกประเภท + ปักหมุด → ส่ง
5. ได้โทเค็น!
6. กด "ยืนยันรายงาน" → ช่วยตรวจสอบของคนอื่น → ได้โทเค็นเพิ่ม!
7. กด "Heatmap" → ดูพื้นที่เสี่ยง

---

## 🌟 สรุป

ระบบนี้ **ไม่ใช่แค่เว็บแจ้งเหตุธรรมดา** แต่เป็น:

✅ **ระบบอัจฉริยะ** ที่ใช้ AI คัดกรอง  
✅ **ระบบชุมชน** ที่ให้คนช่วยกันยืนยัน  
✅ **ระบบ Visualization** ที่แสดง Heatmap  

= **ระบบรายงานภัยสิ่งแวดล้อมอัจฉริยะ** 🚀

---

**สร้างโดย**: Claude (Anthropic)  
**วันที่**: 2025  
**เวอร์ชัน**: 3.0 - Smart Reporting System# 🔥 ไม้ขีด - ระบบรายงานภัยสิ่งแวดล้อมอัจฉริยะ

## 🎯 ภาพรวม

ไม่ใช่แค่เว็บแจ้งเหตุธรรมดา! นี่คือ **ระบบอัจฉริยะ** ที่รวม 3 เทคโนโลยีหลักเข้าด้วยกัน:

### ✨ ฟีเจอร์หลักทั้ง 3 อย่าง

#### 1. 🤖 AI Image Screening (คัดกรองรูปภาพด้วย AI)
- ใช้ **Claude API** วิเคราะห์รูปภาพอัตโนมัติ
- ตรวจจับ: ควัน, ไฟ, เปลวไฟ, การเผา, มลพิษทางอากาศ
- แสดงผลลัพธ์:
  - ✅ **ตรวจพบภัย** (Confidence ≥ 70%) - อนุญาตให้ส่งรายงาน
  - ⚠️ **พบภัยไม่แน่ชัด** (Confidence 40-69%) - แนะนำตรวจสอบเพิ่ม
  - ❌ **ไม่พบภัย** (Confidence < 40%) - บล็อกไม่ให้ส่ง
- ลดรายงานปลอมได้อย่างมีประสิทธิภาพ

#### 2. ✅ Community Verification (ยืนยันจากชุมชน)
- **ให้ผู้ใช้คนอื่นช่วยยืนยัน** รายงานที่ผ่าน AI แล้ว
- รับ **5 โทเค็น** ทุกครั้งที่ยืนยัน
- ต้องมีคนยืนยัน **อย่างน้อย 2 คน** ถึงจะถือว่ารายงานนั้น "verified"
- กรองรายงาน:
  - ผ่าน AI แล้ว
  - ยังไม่ถึง 2 คนยืนยัน  
  - ไม่ใช่รายงานของตัวเอง
- แสดง Badge จำนวนรายงานที่รอการยืนยัน

#### 3. 🔥 Heatmap (แผนที่ความร้อน)
- ใช้ **Leaflet.heat** แสดงความหนาแน่นของจุดรายงาน
- สีเปลี่ยนตามความหนาแน่น:
  - 🔵 สีน้ำเงิน = น้อย
  - 🟡 สีเหลือง = ปานกลาง
  - 🔴 สีแดง = มาก
- มี Legend อธิบายสี
- สลับ เปิด/ปิด ได้

---

## 🚀 วิธีการทำงาน

### ขั้นตอนการแจ้งเหตุ

1. **เลือกประเภทเหตุการณ์**: เผาขยะ, ไฟป่า, ควันรถ, โรงงาน
2. **ถ่ายรูป/อัปโหลดรูป**: จำเป็นต้องมี!
3. **AI วิเคราะห์อัตโนมัติ**: 
   - แสดงผล loading animation
   - ส่งรูปไปที่ Claude API
   - รับผลการวิเคราะห์ภายใน 2-3 วินาที
   - แสดงผลลัพธ์ พร้อมความมั่นใจ
4. **ปักหมุดตำแหน่ง**: ใช้ GPS หรือลากหมุดปรับเอง
5. **ส่งรายงาน**: ถ้าผ่าน AI (confidence ≥ 40%)

### ขั้นตอนการยืนยันจากชุมชน

1. **กดปุ่ม "ยืนยันรายงาน"**
2. **เห็นรายการ** รายงานที่:
   - ผ่าน AI screening แล้ว
   - ยังไม่มีคนยืนยันครบ 2 คน
   - ไม่ใช่รายงานของตัวเอง
3. **ดูรูปภาพ** + ผลการวิเคราะห์ของ AI
4. **เลือก**:
   - ✅ **ยืนยัน** → ได้ 5 โทเค็น
   - ❌ **รายงานผิด** → ไม่ได้โทเค็น

### ขั้นตอนการดู Heatmap

1. **กดปุ่ม "Heatmap"** 
2. **ดูพื้นที่เสี่ยง** ที่มีรายงานหนาแน่น
3. **สังเกตสี**: น้ำเงิน → เหลือง → แดง
4. **กดอีกครั้ง** เพื่อปิด

---

## 📊 ระบบคะแนนและรางวัล

### การได้โทเค็น
- แจ้งเหตุที่ผ่าน AI: **15-25 โทเค็น** (สุ่ม)
- ยืนยันรายงานของคนอื่น: **5 โทเค็น**

### แลกของรางวัล
- ส่วนลดค่าไฟ 20 บาท: **50 โทเค็น**
- บัตรของขวัญ 50 บาท: **100 โทเค็น**
- ปลูกต้นไม้ 1 ต้น: **200 โทเค็น**

---

## 🎨 ไฟล์ในระบบ

```
📁 ระบบไม้ขีด
├── 📄 index.html              # หน้าเว็บหลัก (มี AI, Verification, Heatmap)
├── 📄 app.js                  # JavaScript (AI API, Heatmap, Verification)
├── 📄 style.css               # CSS หลัก
├── 📄 store_styles.css        # CSS ร้านค้า
├── 📄 extended_styles.css     # CSS เพิ่มเติม (AI, Verification, Heatmap)
└── 📄 README.md               # ไฟล์นี้
```

---

## 🔧 เทคโนโลยีที่ใช้

### Frontend
- **HTML5** + **CSS3** (Glassmorphism Design)
- **Vanilla JavaScript** (ไม่ใช้ framework)
- **Leaflet.js** - แผนที่
- **Leaflet.heat** - Heatmap

### AI & Backend
- **Claude API (Sonnet 4)** - วิเคราะห์รูปภาพ
- **Google Apps Script** - Backend (OTP, บันทึกข้อมูล)
- **Google Sheets** - Database

---

## ⚙️ การตั้งค่าก่อนใช้งาน

### 1. ตั้งค่า Anthropic API Key

เปิดไฟล์ `app.js` แล้วแก้ไขบรรทัดที่มี:

```javascript
"x-api-key": "ANTHROPIC_API_KEY_PLACEHOLDER"
```

เป็น:

```javascript
"x-api-key": "sk-ant-api03-xxx_YOUR_ACTUAL_API_KEY_HERE"
```

**วิธีได้ API Key:**
1. ไปที่ https://console.anthropic.com/
2. สมัครสมาชิก (ถ้ายังไม่มีบัญชี)
3. ไปที่ Settings → API Keys
4. สร้าง API Key ใหม่
5. คัดลอกมาใส่ในโค้ด

**⚠️ สำคัญ:** API Key นี้เป็นความลับ! อย่าแชร์หรือ commit ขึ้น Git

### 2. ตั้งค่า Google Apps Script (สำหรับ OTP & Database)

ไฟล์ที่ต้องแก้:
- `app.js` → แก้ `SHEET_URL` ให้ตรงกับ Google Apps Script URL ของคุณ

---

## 🎮 การใช้งาน (หลังตั้งค่าเรียบร้อย)

### Features
- 📱 Responsive Design
- 🎨 Modern Glassmorphism UI
- 🔐 OTP Login System
- 📍 GPS Integration
- 🗺️ Interactive Map
- 🔥 Real-time Heatmap
- 🤖 AI-Powered Screening
- ✅ Community Verification

### ขั้นตอน

1. เปิดเว็บ → Login ด้วย OTP
2. กด "แจ้งเหตุทันที"
3. อัปโหลดรูป → รอ AI วิเคราะห์ (2-3 วินาที)
4. ถ้าผ่าน → เลือกประเภท + ปักหมุด → ส่ง
5. ได้โทเค็น!
6. กด "ยืนยันรายงาน" → ช่วยตรวจสอบของคนอื่น → ได้โทเค็นเพิ่ม!
7. กด "Heatmap" → ดูพื้นที่เสี่ยง

---

## 💡 จุดเด่นของระบบ

### 1. ลดรายงานปลอม
- AI คัดกรองก่อนส่ง
- ชุมชนยืนยันอีกชั้น
- ระบบ 2 ชั้นทำให้ข้อมูลน่าเชื่อถือ

### 2. กระตุ้นการมีส่วนร่วม
- ได้โทเค็นจากการยืนยัน
- ทำให้คนอยากช่วยตรวจสอบ
- สร้างชุมชนที่ช่วยเหลือกัน

### 3. มองเห็นภาพรวม
- Heatmap แสดงพื้นที่เสี่ยงชัดเจน
- Stats แสดงผลแบบ real-time
- ช่วยในการวางแผนแก้ปัญหา

---

## 📈 สถิติที่แสดง

- **รายงานทั้งหมด**: จำนวนรายงานทั้งหมดในระบบ
- **ยืนยันแล้ว**: รายงานที่มีคนยืนยัน ≥ 2 คน
- **ความแม่นยำ AI**: % ของรายงานที่ยืนยันแล้ว / รายงานทั้งหมด

---

## 🎯 การใช้งาน

1. เปิดเว็บ → Login ด้วย OTP
2. กด "แจ้งเหตุทันที"
3. อัปโหลดรูป → รอ AI วิเคราะห์
4. ถ้าผ่าน → เลือกประเภท + ปักหมุด → ส่ง
5. ได้โทเค็น!
6. กด "ยืนยันรายงาน" → ช่วยตรวจสอบของคนอื่น → ได้โทเค็นเพิ่ม!
7. กด "Heatmap" → ดูพื้นที่เสี่ยง

---

## 🌟 สรุป

ระบบนี้ **ไม่ใช่แค่เว็บแจ้งเหตุธรรมดา** แต่เป็น:

✅ **ระบบอัจฉริยะ** ที่ใช้ AI คัดกรอง  
✅ **ระบบชุมชน** ที่ให้คนช่วยกันยืนยัน  
✅ **ระบบ Visualization** ที่แสดง Heatmap  

= **ระบบรายงานภัยสิ่งแวดล้อมอัจฉริยะ** 🚀

---

**สร้างโดย**: Claude (Anthropic)  
**วันที่**: 2025  
**เวอร์ชัน**: 3.0 - Smart Reporting System
# ⚙️ วิธีตั้งค่า API สำหรับระบบไม้ขีด

## 🔑 Anthropic API Key (สำหรับ AI Screening)

### ขั้นตอนที่ 1: สมัครและรับ API Key

1. ไปที่ https://console.anthropic.com/
2. สมัครสมาชิก (ถ้ายังไม่มีบัญชี)
3. ไปที่เมนู **Settings** → **API Keys**
4. คลิก **"Create Key"**
5. คัดลอก API Key ที่ได้ (จะขึ้นต้นด้วย `sk-ant-api03-...`)

### ขั้นตอนที่ 2: ใส่ API Key ในโค้ด

เปิดไฟล์ `app.js` แล้วค้นหาบรรทัดนี้:

```javascript
"x-api-key": "ANTHROPIC_API_KEY_PLACEHOLDER"
```

แก้เป็น:

```javascript
"x-api-key": "sk-ant-api03-YOUR_ACTUAL_KEY_HERE"
```

**ตัวอย่าง:**
```javascript
headers: {
  "Content-Type": "application/json",
  "x-api-key": "sk-ant-api03-abcdefghijk1234567890..."
}
```

### ⚠️ ข้อควรระวัง

1. **อย่าแชร์ API Key ของคุณ** - เป็นความลับส่วนตัว
2. **อย่า commit API Key ขึ้น Git** - ใช้ environment variables แทน
3. **ตรวจสอบ usage** ที่ console.anthropic.com เป็นประจำ
4. **API มีค่าใช้จ่าย** - ตรวจสอบราคาที่ https://www.anthropic.com/pricing

---

## 📊 Google Apps Script (สำหรับ OTP & Database)

### ขั้นตอนที่ 1: สร้าง Google Sheet

1. สร้าง Google Sheet ใหม่
2. ตั้งชื่อว่า "ไม้ขีด - Database"
3. สร้าง columns:
   - `username`
   - `mission_type`
   - `token`
   - `lat`
   - `lng`
   - `image`
   - `ai_approved`
   - `ai_confidence`
   - `ai_description`
   - `verified_count`
   - `timestamp`

### ขั้นตอนที่ 2: สร้าง Apps Script

1. ใน Google Sheet → **Extensions** → **Apps Script**
2. วางโค้ดนี้:

```javascript
function doGet(e) {
  const sheet = SpreadsheetApp.getActiveSpreadsheet().getActiveSheet();
  const data = sheet.getDataRange().getValues();
  const headers = data[0];
  const rows = data.slice(1);
  
  const result = rows.map(row => {
    const obj = {};
    headers.forEach((header, index) => {
      obj[header] = row[index];
    });
    return obj;
  });
  
  return ContentService.createTextOutput(JSON.stringify(result))
    .setMimeType(ContentService.MimeType.JSON);
}

function doPost(e) {
  const sheet = SpreadsheetApp.getActiveSpreadsheet().getActiveSheet();
  const data = JSON.parse(e.postData.contents);
  
  if (data.action === "send_otp") {
    // ส่ง OTP ผ่าน Email
    MailApp.sendEmail({
      to: data.email,
      subject: "รหัส OTP สำหรับไม้ขีด",
      body: `รหัส OTP ของคุณคือ: ${data.otp}\n\nรหัสนี้จะหมดอายุใน 60 วินาที`
    });
    return ContentService.createTextOutput("OTP sent");
  }
  
  if (data.action === "verify_report") {
    // อัพเดท verified_count
    const rows = sheet.getDataRange().getValues();
    for (let i = 1; i < rows.length; i++) {
      if (rows[i][10] === data.report_id) { // timestamp column
        const currentCount = rows[i][9] || 0; // verified_count column
        sheet.getRange(i + 1, 10).setValue(currentCount + 1);
        break;
      }
    }
    return ContentService.createTextOutput("Verified");
  }
  
  // บันทึกรายงานใหม่
  sheet.appendRow([
    data.username,
    data.mission_type,
    data.token,
    data.lat,
    data.lng,
    data.image || "",
    data.ai_approved || false,
    data.ai_confidence || 0,
    data.ai_description || "",
    data.verified_count || 0,
    data.timestamp || new Date().toISOString()
  ]);
  
  return ContentService.createTextOutput("Success");
}
```

### ขั้นตอนที่ 3: Deploy

1. คลิก **Deploy** → **New deployment**
2. เลือก **Web app**
3. ตั้งค่า:
   - Execute as: **Me**
   - Who has access: **Anyone**
4. คลิก **Deploy**
5. คัดลอก **Web app URL**

### ขั้นตอนที่ 4: ใส่ URL ในโค้ด

เปิดไฟล์ `app.js` แล้วค้นหา:

```javascript
const SHEET_URL = "https://script.google.com/macros/s/...";
```

แก้เป็น URL ที่คุณคัดลอกมา

---

## ✅ ทดสอบการทำงาน

### ทดสอบ AI Screening:
1. เปิดเว็บ
2. Login
3. แจ้งเหตุ → อัปโหลดรูปควัน/ไฟ
4. ดูว่า AI วิเคราะห์ถูกต้องหรือไม่

### ทดสอบ Community Verification:
1. สร้างรายงาน 1 อัน
2. Login ด้วย user อื่น
3. กดยืนยันรายงาน
4. ตรวจสอบว่าได้ 5 โทเค็น

### ทดสอบ Heatmap:
1. สร้างรายงานหลายจุด
2. กดปุ่ม Heatmap
3. ดูว่าแสดงสีตามความหนาแน่น

---

## 🐛 แก้ปัญหา

### AI ไม่ทำงาน
- ✅ ตรวจสอบ API Key ใส่ถูกหรือไม่
- ✅ เช็ค Console (F12) ดู error
- ✅ ตรวจสอบ API quota

### OTP ไม่มา
- ✅ ตรวจสอบ Apps Script Deploy แล้วหรือยัง
- ✅ เช็คว่า Gmail มีสิทธิ์ส่ง Email หรือไม่

### Heatmap ไม่แสดง
- ✅ ต้องมีข้อมูลรายงานอย่างน้อย 1 รายการ
- ✅ ตรวจสอบว่า lat/lng ถูกต้อง

---

## 📞 ติดต่อสอบถาม

หากมีปัญหาหรือข้อสงสัย กรุณาเช็คที่:
- README.md
- Console.log (F12)
- Anthropic Documentation: https://docs.anthropic.com/

Good luck! 🚀